<div class="url-results">
    <h2 class="text-xl mb-4 text-gray-800  dark:text-gray-300">URL's acortadas</h2>

            {{-- <div>
        <h2>Shortened URLs</h2>
        <ul>
            @foreach ($urls as $url)
                <li>
                    <a href="{{ $url->shortened }}">{{ $url->shortened }}</a>
                    <span>({{ $url->original }})</span>
                </li>
            @endforeach
        </ul>
    </div> --}}

    <x-tabla />
</div>